{
	"name": "thin-ql",
	"version": "0.1.0",
	"description": "Making crud apis disappear.",
	"main": "./src/index.ts",
	"scripts": {
		"main": "ts-node ./main.ts",
		"main:sql": "PGPASSWORD='asdf' psql -h localhost -U experiment_user experiment_db -t -f ./main.sql | jq",
		"inspect": "ts-node -e 'import { inspect } from \"./src/inspect\"; inspect({ user: `experiment_user`, password: `asdf`, database: `experiment_db`, host: `localhost`, port: 5432 })'",
		"inspect:sql": "PGPASSWORD='asdf' psql -h localhost -U experiment_user experiment_db -t -f src/inspect.sql | jq",
		"test": "npm run build:templates && mocha --recursive -r ts-node/register src/**/*.spec.ts src/**/**/*.spec.ts",
		"build:templates": "topside src/**/*.sql && ts-node unescape.ts",
		"build": "npm run build:templates && rm -Rf dist/* && tsc --build tsconfig.json",
		"clean": "rm -f src/sql/*.ts*"
	},
	"author": "",
	"license": "MIT",
	"devDependencies": {
		"@types/chai": "^4.1.7",
		"@types/glob": "^7.1.1",
		"@types/mocha": "^5.2.6",
		"@types/node": "^11.13.13",
		"@types/pg": "^7.4.14",
		"base64url": "^3.0.1",
		"chai": "^4.2.0",
		"glob": "^7.1.6",
		"kreia": "^0.3.2",
		"mocha": "^5.2.0",
		"pascal-case": "^2.0.1",
		"pg": "^7.11.0",
		"snake-case": "^2.1.0",
		"topside": "^0.6.5",
		"ts-node": "^8.2.0",
		"typescript": "^3.5.1"
	},
	"dependencies": {
		"chalk": "^2.4.2"
	}
}
